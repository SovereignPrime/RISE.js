'use strict'

    /*const IPFS = require('ipfs')

const node = new IPFS()

node.once('ready', () => {
    return node.repo.stat(data => data);
});*/

module.exports = {
    stats: "node.repo.stat",
}
